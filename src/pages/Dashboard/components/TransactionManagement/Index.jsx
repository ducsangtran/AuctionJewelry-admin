import { Outlet } from "react-router-dom";

export const TransactionManagement = () => {
    return (
        <div>
            <h1>Transactions Management</h1>
            <Outlet />
        </div>
    );
};
